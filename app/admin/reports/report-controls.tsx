"use client";
export function ReportControls({from,to,rows}:{from:string;to:string;rows:any[]}){function exportCsv(){const csv=[['Order','Status','Total','Currency','Created'],...rows.map(r=>[r.order_number,r.status,r.total,r.currency,r.created_at])].map(r=>r.map(v=>`"${String(v).replaceAll('"','""')}"`).join(',')).join('\n');const blob=new Blob([csv],{type:'text/csv'});const a=document.createElement('a');a.href=URL.createObjectURL(blob);a.download='sites-bd-report.csv';a.click();URL.revokeObjectURL(a.href)}return <form className="grid gap-3 rounded-[--radius-surface] border border-gray-200 bg-white p-4 sm:grid-cols-[1fr_1fr_auto_auto]" method="get"><input type="date" name="from" defaultValue={from} className="rounded-xl border px-4 py-3"/><input type="date" name="to" defaultValue={to} className="rounded-xl border px-4 py-3"/><button className="rounded-xl bg-slate-900 px-4 py-3 text-sm font-bold text-white">Apply</button><button type="button" onClick={exportCsv} className="rounded-xl border px-4 py-3 text-sm font-bold">Export CSV</button></form>}
