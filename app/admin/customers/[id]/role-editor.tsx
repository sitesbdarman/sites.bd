"use client";
import {useState} from "react";
export function RoleEditor({id,initialRole}:{id:string;initialRole:string}){const[role,setRole]=useState(initialRole);const[busy,setBusy]=useState(false);const[msg,setMsg]=useState("");async function save(){setBusy(true);setMsg("");try{const r=await fetch(`/api/admin/users/${id}/role`,{method:"PATCH",headers:{"Content-Type":"application/json"},body:JSON.stringify({role})});const d=await r.json();if(!r.ok)throw new Error(d.error||"Could not update role");setMsg("Role updated.")}catch(e){setMsg(e instanceof Error?e.message:"Could not update role.")}finally{setBusy(false)}}return <div className="rounded-2xl border bg-white p-5 shadow-sm"><div className="text-sm font-bold text-slate-500">Staff role</div><div className="mt-3 flex flex-wrap gap-3"><select value={role} onChange={e=>setRole(e.target.value)} className="rounded-xl border px-4 py-2"><option value="user">User</option><option value="support_agent">Support Agent</option><option value="finance">Finance</option><option value="admin">Admin</option><option value="super_admin">Super Admin</option></select><button disabled={busy} onClick={save} className="rounded-xl bg-slate-900 px-4 py-2 text-sm font-bold text-white">{busy?"Saving...":"Save role"}</button></div>{msg&&<p className="mt-2 text-sm text-slate-500">{msg}</p>}</div>}
