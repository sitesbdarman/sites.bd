import { requireAdmin } from "@/lib/admin/auth";
import { createAdminClient } from "@/lib/supabase/admin";
export default async function AdminOrdersPage(){ await requireAdmin(); const db=createAdminClient(); const {data}=await db.from("orders").select("id,order_number,customer_id,status,currency,total,created_at").order("created_at",{ascending:false}); return <section><h1 className="text-2xl font-bold">Orders</h1><div className="mt-6 overflow-x-auto rounded-xl border bg-white shadow-sm"><table className="w-full text-left text-sm"><thead className="bg-gray-50 text-xs uppercase text-gray-500"><tr>{['Order','Customer','Status','Total','Date'].map(x=><th key={x} className="px-5 py-3">{x}</th>)}</tr></thead><tbody className="divide-y">{(data||[]).map((o:any)=><tr key={o.id}><td className="px-5 py-4 font-medium">{o.order_number}</td><td className="px-5 py-4 text-xs">{o.customer_id}</td><td className="px-5 py-4">{o.status}</td><td className="px-5 py-4">{o.currency} {Number(o.total).toFixed(2)}</td><td className="px-5 py-4">{new Date(o.created_at).toLocaleDateString()}</td></tr>)}</tbody></table></div></section>}
