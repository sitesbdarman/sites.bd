"use client";
import {useMemo,useState} from "react";
type Coupon={id:string;code:string;discount_type:string;discount_value:number|string;min_order_amount:number|string;max_discount_amount:number|string|null;starts_at:string|null;ends_at:string|null;usage_limit:number|null;usage_count:number;active:boolean;created_at:string};
export function CouponManager({initialCoupons}:{initialCoupons:Coupon[]}){const[coupons,setCoupons]=useState(initialCoupons);const[q,setQ]=useState("");const[status,setStatus]=useState("all");const[busy,setBusy]=useState(false);const filtered=useMemo(()=>coupons.filter(c=>(!q||c.code.toLowerCase().includes(q.toLowerCase()))&&(status==='all'||(status==='active'?c.active:!c.active))),[coupons,q,status]);
 async function toggle(c:Coupon){setBusy(true);const r=await fetch('/api/admin/coupons',{method:'PATCH',headers:{'Content-Type':'application/json'},body:JSON.stringify({id:c.id,active:!c.active})});const d=await r.json();if(r.ok)setCoupons(xs=>xs.map(x=>x.id===c.id?d.coupon:x));setBusy(false)}
 async function remove(c:Coupon){if(!confirm(`Delete ${c.code}?`))return;setBusy(true);const r=await fetch(`/api/admin/coupons?id=${c.id}`,{method:'DELETE'});if(r.ok)setCoupons(xs=>xs.filter(x=>x.id!==c.id));setBusy(false)}
 function exportCsv(){const csv=[['Code','Type','Value','Min Order','Usage','Status'],...filtered.map(c=>[c.code,c.discount_type,c.discount_value,c.min_order_amount,`${c.usage_count}/${c.usage_limit??'∞'}`,c.active?'active':'disabled'])].map(r=>r.map(v=>`"${String(v).replaceAll('"','""')}"`).join(',')).join('\n');const blob=new Blob([csv],{type:'text/csv'});const a=document.createElement('a');a.href=URL.createObjectURL(blob);a.download='sites-bd-coupons.csv';a.click();URL.revokeObjectURL(a.href)}
 return <section className="space-y-5"><div className="flex flex-wrap items-end justify-between gap-4"><div><h1 className="text-2xl font-black">Coupons</h1><p className="mt-1 text-sm text-slate-500">Search, export and manage promotional codes.</p></div><button onClick={exportCsv} className="rounded-xl border px-4 py-2.5 text-sm font-bold">Export CSV</button></div><div className="rounded-2xl border bg-white p-4 shadow-sm grid gap-3 sm:grid-cols-2"><input value={q} onChange={e=>setQ(e.target.value)} placeholder="Search coupon code" className="rounded-xl border px-4 py-3"/><select value={status} onChange={e=>setStatus(e.target.value)} className="rounded-xl border px-4 py-3"><option value="all">All</option><option value="active">Active</option><option value="disabled">Disabled</option></select></div><div className="overflow-x-auto rounded-2xl border bg-white shadow-sm"><table className="w-full min-w-[900px] text-left text-sm"><thead className="bg-slate-50 text-xs uppercase text-slate-500"><tr>{['Code','Discount','Minimum','Usage','Status','Actions'].map(x=><th key={x} className="px-5 py-3">{x}</th>)}</tr></thead><tbody className="divide-y">{filtered.map(c=><tr key={c.id}><td className="px-5 py-4 font-semibold">{c.code}</td><td className="px-5 py-4">{c.discount_type==='percent'?`${c.discount_value}%`:`BDT ${Number(c.discount_value).toFixed(2)}`}</td><td className="px-5 py-4">BDT {Number(c.min_order_amount).toFixed(2)}</td><td className="px-5 py-4">{c.usage_count} / {c.usage_limit??'∞'}</td><td className="px-5 py-4">{c.active?'Active':'Disabled'}</td><td className="px-5 py-4"><div className="flex gap-2"><button disabled={busy} onClick={()=>toggle(c)} className="rounded-lg border px-3 py-1.5">{c.active?'Disable':'Enable'}</button><button disabled={busy} onClick={()=>remove(c)} className="rounded-lg border border-red-200 px-3 py-1.5 text-red-600">Delete</button></div></td></tr>)}{filtered.length===0&&<tr><td colSpan={6} className="px-5 py-10 text-center text-slate-500">No coupons found.</td></tr>}</tbody></table></div></section>}
