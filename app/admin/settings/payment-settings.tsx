"use client";
import {useState} from "react";
export function PaymentSettings({initial}:{initial:{bkash_number:string;nagad_number:string;rocket_number:string}}){const [form,setForm]=useState(initial);const[busy,setBusy]=useState(false);const[msg,setMsg]=useState("");async function save(){setBusy(true);setMsg("");try{const r=await fetch("/api/admin/payment-settings",{method:"PATCH",headers:{"Content-Type":"application/json"},body:JSON.stringify(form)});const d=await r.json();if(!r.ok||!d.success)throw new Error(d.error||"Could not save settings.");setForm(d.settings);setMsg("Payment settings saved.");}catch(e){setMsg(e instanceof Error?e.message:"Could not save settings.");}finally{setBusy(false);}}return <section className="max-w-2xl"><h1 className="text-2xl font-bold text-gray-900">Payment Settings</h1><p className="mt-1 text-sm text-gray-500">Change the bKash, Nagad and Rocket receiving numbers without redeploying.</p><div className="mt-6 rounded-xl border bg-white p-5 space-y-4">{([["bkash_number","bKash"],["nagad_number","Nagad"],["rocket_number","Rocket"]] as const).map(([key,label])=><label key={key} className="block text-sm font-medium text-gray-700">{label} number<input value={form[key]} onChange={e=>setForm({...form,[key]:e.target.value})} placeholder="01XXXXXXXXX" className="mt-1 w-full rounded-md border px-3 py-2"/></label>)}{msg&&<p className="text-sm text-gray-600">{msg}</p>}<button onClick={save} disabled={busy} className="rounded-md bg-gray-900 px-4 py-2 text-sm font-medium text-white disabled:opacity-50">{busy?"Saving...":"Save Payment Settings"}</button></div></section>}
