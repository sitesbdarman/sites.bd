"use client"; import {useState} from "react"; export function AdminTicketEditor({ticket,replies}:{ticket:any;replies:any[]}){const [status,setStatus]=useState(ticket.status);const [message,setMessage]=useState("");const [notice,setNotice]=useState("");const [busy,setBusy]=useState(false);async function save(){setBusy(true);const r=await fetch(`/api/admin/tickets/${ticket.id}`,{method:"PATCH",headers:{"Content-Type":"application/json"},body:JSON.stringify({status,message:message||undefined})});const j=await r.json();setNotice(r.ok?"Updated successfully.":j.error||"Update failed");if(r.ok)setMessage("");setBusy(false)}return <section><div className="flex flex-wrap items-center justify-between gap-3"><div><p className="text-xs text-gray-400">{ticket.ticket_number}</p><h1 className="text-2xl font-black">{ticket.subject}</h1></div><select value={status} onChange={e=>setStatus(e.target.value)} className="rounded-xl border px-3 py-2 text-sm"><option value="open">Open</option><option value="pending">Processing</option><option value="resolved">Resolved</option><option value="closed">Closed</option></select></div><div className="mt-5 rounded-[--radius-surface] border border-gray-200 bg-white p-5 space-y-3"><div className="rounded-xl bg-gray-50 p-4 text-sm"><p className="text-xs font-bold text-gray-500">Customer request</p><p className="mt-2 whitespace-pre-wrap">{ticket.message}</p></div>{replies.map(r=><div key={r.id} className={`rounded-xl p-4 text-sm ${r.is_admin?"bg-blue-50":"bg-gray-50"}`}><p className="text-xs font-bold text-gray-500">{r.is_admin?"Admin":"Customer"}</p><p className="mt-2 whitespace-pre-wrap">{r.message}</p></div>)}<textarea value={message} onChange={e=>setMessage(e.target.value)} className="min-h-32 w-full rounded-xl border p-3" placeholder="Admin reply (optional)"/><button onClick={()=>void save()} disabled={busy} className="rounded-xl bg-blue-600 px-5 py-3 text-sm font-bold text-white">{busy?"Saving…":"Save / Reply"}</button>{notice&&<p className="text-sm text-gray-500">{notice}</p>}</div></section>}
