import { requireAdmin } from "@/lib/admin/auth";
import { createAdminClient } from "@/lib/supabase/admin";
export default async function AdminTicketsPage(){ await requireAdmin(); const db=createAdminClient(); const {data}=await db.from("support_tickets").select("id,ticket_number,customer_id,subject,status,priority,created_at").order("created_at",{ascending:false}); return <section><h1 className="text-2xl font-bold">Support Tickets</h1><div className="mt-6 overflow-x-auto rounded-xl border bg-white shadow-sm"><table className="w-full text-left text-sm"><thead className="bg-gray-50 text-xs uppercase text-gray-500"><tr>{['Ticket','Customer','Subject','Priority','Status','Created'].map(x=><th key={x} className="px-5 py-3">{x}</th>)}</tr></thead><tbody className="divide-y">{(data||[]).map((t:any)=><tr key={t.id}><td className="px-5 py-4 font-medium"><a className="text-blue-600 hover:underline" href={`/admin/tickets/${t.id}`}>{t.ticket_number}</a></td><td className="px-5 py-4 text-xs">{t.customer_id}</td><td className="px-5 py-4">{t.subject}</td><td className="px-5 py-4">{t.priority}</td><td className="px-5 py-4">{t.status}</td><td className="px-5 py-4">{new Date(t.created_at).toLocaleDateString()}</td></tr>)}</tbody></table></div></section>}
