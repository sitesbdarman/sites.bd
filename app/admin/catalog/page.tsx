"use client";
import { useEffect, useState } from "react";
type Kind="hosting"|"addons"|"domains";
type Row=Record<string,any>;
const empty={hosting:{name:"New Hosting Plan",type:"premium",price:0,billing_cycle:"yearly",description:"",is_active:true,sort_order:999},addons:{name:"New Add-on",description:"",price:0,billing_cycle:"one_time",currency:"BDT",is_active:true,sort_order:999},domains:{tld:"com",registration_price:0,renewal_price:0,currency:"USD",is_active:true,sort_order:999}};
function Field({label,children}:{label:string;children:React.ReactNode}){return <label className="block"><span className="text-xs font-bold text-slate-500">{label}</span>{children}</label>}
export default function CatalogPage(){const [kind,setKind]=useState<Kind>("hosting"),[items,setItems]=useState<Row[]>([]),[loading,setLoading]=useState(true),[saving,setSaving]=useState(false),[msg,setMsg]=useState("");
async function load(){setLoading(true);setMsg("");try{const r=await fetch(`/api/admin/catalog?kind=${kind}`,{cache:"no-store"});const d=await r.json();if(!r.ok)throw new Error(d.error);setItems(d.items||[])}catch(e){setMsg(e instanceof Error?e.message:"Could not load catalog.")}finally{setLoading(false)}}
useEffect(()=>{load()},[kind]); const update=(i:number,p:Row)=>setItems(x=>x.map((r,n)=>n===i?{...r,...p}:r));
async function save(){setSaving(true);setMsg("");try{const r=await fetch("/api/admin/catalog",{method:"PUT",headers:{"Content-Type":"application/json"},body:JSON.stringify({kind,items})});const d=await r.json();if(!r.ok)throw new Error(d.error);setItems(d.items||[]);setMsg("Saved successfully.")}catch(e){setMsg(e instanceof Error?e.message:"Could not save.")}finally{setSaving(false)}}
const title=kind==="hosting"?"Hosting plans":kind==="addons"?"Checkout add-ons":"Domain TLD pricing";
return <section className="space-y-6"><div className="rounded-3xl bg-gradient-to-br from-slate-950 via-slate-900 to-sky-950 p-7 text-white"><p className="text-xs font-bold uppercase tracking-[.2em] text-sky-300">Business catalog</p><h1 className="mt-2 text-3xl font-black">{title}</h1><p className="mt-2 max-w-2xl text-sm leading-6 text-slate-300">Add, edit, activate, deactivate or remove customer-facing commercial items without changing source code.</p></div>
<div className="flex flex-wrap gap-2">{([['hosting','Hosting'],['addons','Add-ons'],['domains','Domain TLDs']] as [Kind,string][]).map(([k,l])=><button key={k} onClick={()=>setKind(k)} className={`rounded-xl px-4 py-2.5 text-sm font-bold ${kind===k?'bg-sky-600 text-white':'border border-slate-200 bg-white text-slate-700'}`}>{l}</button>)}</div>
{msg&&<div className="rounded-xl border border-sky-200 bg-sky-50 px-4 py-3 text-sm font-semibold text-sky-800">{msg}</div>}
{loading?<div className="surface p-8 text-slate-500">Loading…</div>:<div className="space-y-4">{items.map((r,i)=><div key={r.id||i} className="surface p-5"><div className="grid gap-4 md:grid-cols-4">{kind==="hosting"&&<><Field label="Name"><input value={r.name||""} onChange={e=>update(i,{name:e.target.value})} className="mt-1 w-full rounded-xl border px-3 py-2.5"/></Field><Field label="Type"><select value={r.type||"premium"} onChange={e=>update(i,{type:e.target.value})} className="mt-1 w-full rounded-xl border px-3 py-2.5"><option>premium</option><option>free</option><option>custom</option></select></Field><Field label="Price"><input type="number" value={r.price??0} onChange={e=>update(i,{price:Number(e.target.value)})} className="mt-1 w-full rounded-xl border px-3 py-2.5"/></Field><Field label="Billing"><select value={r.billing_cycle||"yearly"} onChange={e=>update(i,{billing_cycle:e.target.value})} className="mt-1 w-full rounded-xl border px-3 py-2.5"><option>monthly</option><option>yearly</option><option>one_time</option><option>n_a</option></select></Field><Field label="Description"><textarea value={r.description||""} onChange={e=>update(i,{description:e.target.value})} className="mt-1 min-h-20 w-full rounded-xl border px-3 py-2.5 md:col-span-3"/></Field></>}{kind==="addons"&&<><Field label="Name"><input value={r.name||""} onChange={e=>update(i,{name:e.target.value})} className="mt-1 w-full rounded-xl border px-3 py-2.5"/></Field><Field label="Price"><input type="number" value={r.price??0} onChange={e=>update(i,{price:Number(e.target.value)})} className="mt-1 w-full rounded-xl border px-3 py-2.5"/></Field><Field label="Currency"><input value={r.currency||"BDT"} onChange={e=>update(i,{currency:e.target.value.toUpperCase()})} className="mt-1 w-full rounded-xl border px-3 py-2.5"/></Field><Field label="Billing"><input value={r.billing_cycle||"one_time"} onChange={e=>update(i,{billing_cycle:e.target.value})} className="mt-1 w-full rounded-xl border px-3 py-2.5"/></Field><Field label="Description"><textarea value={r.description||""} onChange={e=>update(i,{description:e.target.value})} className="mt-1 min-h-20 w-full rounded-xl border px-3 py-2.5 md:col-span-4"/></Field></>}{kind==="domains"&&<><Field label="TLD"><input value={r.tld||""} onChange={e=>update(i,{tld:e.target.value.replace(/^\./,"")})} className="mt-1 w-full rounded-xl border px-3 py-2.5" placeholder="com"/></Field><Field label="Registration"><input type="number" value={r.registration_price??0} onChange={e=>update(i,{registration_price:Number(e.target.value)})} className="mt-1 w-full rounded-xl border px-3 py-2.5"/></Field><Field label="Renewal"><input type="number" value={r.renewal_price??0} onChange={e=>update(i,{renewal_price:Number(e.target.value)})} className="mt-1 w-full rounded-xl border px-3 py-2.5"/></Field><Field label="Currency"><input value={r.currency||"USD"} onChange={e=>update(i,{currency:e.target.value.toUpperCase()})} className="mt-1 w-full rounded-xl border px-3 py-2.5"/></Field></>}</div><div className="mt-4 flex flex-wrap items-center justify-between gap-3 border-t border-slate-100 pt-4"><label className="flex items-center gap-2 text-sm font-bold"><input type="checkbox" checked={Boolean(r.is_active)} onChange={e=>update(i,{is_active:e.target.checked})}/><span>Visible to customers</span></label><button type="button" onClick={()=>setItems(x=>x.filter((_,n)=>n!==i))} className="rounded-lg border border-red-200 px-3 py-1.5 text-xs font-bold text-red-600 hover:bg-red-50">Remove</button></div></div>)}<div className="flex flex-wrap gap-3"><button onClick={()=>setItems(x=>[...x,{...empty[kind]}])} className="rounded-xl border border-slate-300 bg-white px-5 py-3 text-sm font-bold text-slate-700">+ Add {kind==="domains"?"TLD":kind==="hosting"?"hosting plan":"add-on"}</button><button onClick={save} disabled={saving} className="rounded-xl bg-sky-600 px-5 py-3 text-sm font-bold text-white disabled:opacity-60">{saving?"Saving…":"Save changes"}</button></div><p className="text-xs text-slate-500">Removing an item removes it from the active catalog. Existing customer orders remain unchanged.</p></div>}</section>}
