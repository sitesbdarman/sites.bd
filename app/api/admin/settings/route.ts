import {NextResponse} from 'next/server';import {assertSameOrigin} from '@/lib/security/csrf';import {assertAdminApi} from '@/lib/admin/auth';import {createAdminClient} from '@/lib/supabase/admin';
export async function POST(req:Request){const oe=assertSameOrigin(req);if(oe)return oe;const {user,response}=await assertAdminApi('settings:write');if(response)return response;const f=await req.formData();const site={site_name:String(f.get('site_name')||'SITES.BD'),support_email:String(f.get('support_email')||''),support_whatsapp:String(f.get('support_whatsapp')||''),support_hours:String(f.get('support_hours')||'24/7'),maintenance:f.get('maintenance')==='on',site_notice:String(f.get('site_notice')||'')};const policies={terms:String(f.get('terms')||''),refund:String(f.get('refund')||'')};const db=createAdminClient();await db.from('app_config').upsert([{key:'site_settings',value:site,updated_at:new Date().toISOString()},{key:'policies',value:policies,updated_at:new Date().toISOString()}],{onConflict:'key'});await db.from('admin_audit_logs').insert({admin_id:user!.id,action:'settings_updated',entity_type:'settings',metadata:{maintenance:site.maintenance}});return NextResponse.redirect(new URL('/admin/settings?saved=1',req.url));}
