import { NextResponse } from "next/server";
import { z } from "zod";
import { assertAdminApi } from "@/lib/admin/auth";
const schema=z.object({status:z.enum(["open","pending","resolved","closed"]).optional(),message:z.string().trim().min(1).max(5000).optional()});
export async function PATCH(req:Request,{params}:{params:Promise<{id:string}>}){const {user,admin,response}=await assertAdminApi();if(response)return response;const {id}=await params;const body=schema.safeParse(await req.json());if(!body.success)return NextResponse.json({error:"Invalid update"},{status:400});const {status,message}=body.data;if(!status&&!message)return NextResponse.json({error:"Nothing to update"},{status:400});if(status){const {error}=await admin!.from("support_tickets").update({status,updated_at:new Date().toISOString(),closed_at:status==="closed"?new Date().toISOString():null}).eq("id",id);if(error)return NextResponse.json({error:error.message},{status:500})}if(message){const {error}=await admin!.from("support_ticket_replies").insert({ticket_id:id,user_id:user!.id,is_admin:true,message});if(error)return NextResponse.json({error:error.message},{status:500});if(!status){await admin!.from("support_tickets").update({status:"pending",updated_at:new Date().toISOString()}).eq("id",id)}}await admin!.from("admin_audit_logs").insert({admin_id:user!.id,action:"ticket_update",entity_type:"ticket",entity_id:id,metadata:body.data});return NextResponse.json({ok:true})}
