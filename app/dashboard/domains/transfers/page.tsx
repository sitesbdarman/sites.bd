import Link from "next/link"; import { createClient } from "@/lib/supabase/server"; import { DashboardLayout } from "@/components/dashboard/DashboardLayout";
export default async function TransfersPage(){const db=await createClient(); const {data:{user}}=await db.auth.getUser(); return <DashboardLayout pageTitle="Domain Transfers" userEmail={user?.email??null}><div className="rounded-xl border border-gray-200 bg-white p-6"><h2 className="text-xl font-black">Transfer a domain</h2><p className="mt-2 text-sm text-gray-500">Transfer workflows are provider-dependent. When registrar integration is enabled, transfer status and authorization-code steps will appear here.</p><Link href="/contact" className="mt-5 inline-flex rounded-xl bg-gray-900 px-4 py-2.5 text-sm font-bold text-white">Contact Support</Link></div></DashboardLayout>}
