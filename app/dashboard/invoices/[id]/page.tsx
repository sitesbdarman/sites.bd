import { notFound } from "next/navigation"; import Link from "next/link"; import { createClient } from "@/lib/supabase/server"; import { DashboardLayout } from "@/components/dashboard/DashboardLayout";
export default async function InvoiceDetails({params}:{params:Promise<{id:string}>}){const {id}=await params;const db=await createClient();const {data:{user}}=await db.auth.getUser();if(!user)return null;const {data:invoice}=await db.from("invoices").select("*,orders(order_number,hosting_plan_name,hosting_price)").eq("id",id).eq("customer_id",user.id).maybeSingle();if(!invoice)notFound();return <DashboardLayout pageTitle="Invoice Details" userEmail={user.email??null}><Link href="/dashboard/invoices" className="text-sm font-bold text-blue-600">← My Invoices</Link><div className="mt-4 rounded-2xl border bg-white p-6 shadow-sm"><div className="flex flex-wrap justify-between gap-4"><div><p className="text-xs text-gray-400">Invoice</p><h1 className="mt-1 text-2xl font-black">{invoice.invoice_number}</h1></div><span className="rounded-full bg-amber-50 px-3 py-1 text-xs font-bold text-amber-700">{invoice.status}</span></div><div className="mt-6 grid gap-4 sm:grid-cols-3"><div><p className="text-xs text-gray-400">Order</p><p className="mt-1 font-semibold">{(invoice as any).orders?.order_number||"—"}</p></div><div><p className="text-xs text-gray-400">Issued</p><p className="mt-1 font-semibold">{new Date(invoice.created_at).toLocaleDateString()}</p></div><div><p className="text-xs text-gray-400">Total</p><p className="mt-1 text-lg font-black">{invoice.currency} {Number(invoice.total).toFixed(2)}</p></div></div>{invoice.status!=="paid"&&<div className="mt-6 rounded-xl bg-green-50 p-4 text-sm text-green-800">Use the Pay Now option from your payment flow to contact the support/payment channel with this invoice reference.</div>}</div></DashboardLayout>}
