/**
 * Supabase generated database types.
 *
 * ⚠️ THIS FILE IS A PLACEHOLDER, NOT REAL GENERATED OUTPUT.
 *
 * A real Supabase project (with credentials) is required to generate
 * accurate types from the actual schema. Once the project is linked,
 * generate the real file with:
 *
 *   npx supabase login
 *   npx supabase link --project-ref <your-project-ref>
 *   npm run gen:types
 *
 * That command overwrites this file with the CLI's real output. Until
 * then, `Database` is intentionally left minimal/untyped so the app
 * compiles, rather than hand-writing fake types that could silently
 * drift from the real schema in database/0001_foundation.sql.
 */

// eslint-disable-next-line @typescript-eslint/no-explicit-any
export type Database = any;
