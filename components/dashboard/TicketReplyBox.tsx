"use client"; import {useState} from "react"; export function TicketReplyBox({ticketId}:{ticketId:string}){const [message,setMessage]=useState("");const [busy,setBusy]=useState(false);const [notice,setNotice]=useState("");async function send(e:React.FormEvent){e.preventDefault();setBusy(true);const r=await fetch(`/api/tickets/${ticketId}`,{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({message})});const j=await r.json();setNotice(j.error||j.message||"Reply sent.");if(r.ok)setMessage("");setBusy(false)}return <form onSubmit={send} className="mt-5 rounded-[--radius-surface] border border-gray-200 bg-white p-5"><h2 className="font-bold">Reply to ticket</h2><textarea value={message} onChange={e=>setMessage(e.target.value)} className="mt-3 min-h-32 w-full rounded-xl border p-3 text-sm" placeholder="Write your reply…" required/><button disabled={busy} className="mt-3 rounded-xl bg-blue-600 px-5 py-3 text-sm font-bold text-white disabled:opacity-50">{busy?"Sending…":"Send Reply"}</button>{notice&&<p className="mt-2 text-sm text-gray-500">{notice}</p>}</form>}
