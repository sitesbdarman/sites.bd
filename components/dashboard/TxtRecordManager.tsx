"use client";
import { useEffect, useState } from "react";
export function TxtRecordManager({domainId,domainName}:{domainId:string;domainName:string}){
 const [records,setRecords]=useState<any[]>([]); const [name,setName]=useState("@"); const [value,setValue]=useState(""); const [busy,setBusy]=useState(false); const [message,setMessage]=useState("");
 const load=async()=>{const r=await fetch(`/api/domains/${domainId}/txt`,{cache:"no-store"}); const j=await r.json(); setRecords(j.records||[])}; useEffect(()=>{void load()},[domainId]);
 async function submit(e:React.FormEvent){e.preventDefault();setBusy(true);setMessage(""); const r=await fetch(`/api/domains/${domainId}/txt`,{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({name,value})}); const j=await r.json(); setMessage(j.message||j.error||"Done"); if(r.ok){setName("@");setValue("");await load()} setBusy(false)}
 return <div className="grid gap-5 lg:grid-cols-2"><form onSubmit={submit} className="rounded-2xl border bg-white p-5 shadow-sm"><h2 className="font-bold">New TXT record</h2><label className="mt-4 block text-sm font-semibold">Name / Host<input value={name} onChange={e=>setName(e.target.value)} className="mt-2 w-full rounded-xl border px-3 py-2.5" placeholder="@ or vercel" /></label><label className="mt-4 block text-sm font-semibold">Value<input value={value} onChange={e=>setValue(e.target.value)} className="mt-2 w-full rounded-xl border px-3 py-2.5" placeholder={`Verification value containing ${domainName}`} /></label><button disabled={busy} className="mt-5 w-full rounded-xl bg-blue-600 px-4 py-3 font-bold text-white disabled:opacity-50">{busy?"Validating…":"Validate & Add TXT"}</button>{message&&<p className="mt-3 rounded-xl bg-gray-50 p-3 text-sm">{message}</p>}</form><div className="rounded-2xl border bg-white p-5 shadow-sm"><h2 className="font-bold">Current TXT Records</h2><div className="mt-4 space-y-3">{!records.length?<p className="text-sm text-gray-500">No TXT records found.</p>:records.map(r=><div key={r.id} className="rounded-xl bg-gray-50 p-3 text-sm"><div className="font-semibold">{r.name}</div><div className="mt-1 break-all text-gray-600">{r.content}</div></div>)}</div></div></div>
}
